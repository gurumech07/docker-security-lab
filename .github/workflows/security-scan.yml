name: Security Analysis & Publish

on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      
      - name: SonarCloud Scan
        uses: sonarsource/sonarqube-scan-action@v4
        with:
          args: >
            -Dsonar.projectKey=gurumech07_docker-security-lab
            -Dsonar.organization=gurumech07
            -Dsonar.sources=.
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.token=${{ secrets.SONAR_TOKEN }}
            -Dsonar.verbose=true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  comparison:
    name: Security Comparison (Std vs Hardened)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Standard Image
        run: docker build -t security-lab:std -f Dockerfile.std .

      - name: Build Hardened Image
        run: docker build -t security-lab:hardened -f Dockerfile .

      - name: Docker Scout Comparison
        uses: docker/scout-action@v1
        with:
          command: compare
          image: security-lab:hardened
          to: security-lab:std
          ignore-unchanged: false
          only-severities: critical,high
          write-comment: true # Adds a PR comment if applicable
          github-token: ${{ secrets.GITHUB_TOKEN }}
        env:
          DOCKER_HUB_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKER_HUB_PAT: ${{ secrets.DOCKERHUB_TOKEN }}

  build-and-push:
    name: Build and Push
    needs: [sonarcloud, comparison]
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v3

      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Dockerfile

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/security-lab:hardened -f Dockerfile .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/security-lab:hardened
