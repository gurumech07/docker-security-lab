# Standard Dockerfile (Not Hardened)
# Using a heavy base image and running as root
FROM python:3.9

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Running as root (default)
EXPOSE 5000

CMD ["python", "app.py"]
